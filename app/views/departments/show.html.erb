<%= link_to t("actions.edit"), edit_department_path(@department), class: "btn btn-primary pull-right" %>
<%= link_to t("actions.back"), departments_path(current_user), class: "btn btn-primary pull-right" %>
<div class="x_content">
  <div class="row">
    <div class="col-md-6 col-xs-12 col-md-offset-3">
      <p> <label>Nome: </label> <%= @department.name %></p>
      <p> <label>Descrição: </label> <%= @department.description %></p>
    </div>
  </div>
</div>
